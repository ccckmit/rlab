// http://mathjs.org/examples/index.html
var M = {
	PI:Math.PI,
	E:Math.E,	
	abs:Math.abs,
	acos:Math.acos,
	asin:Math.acos,
	atan:Math.atan,
	ceil:Math.ceil,
	cos:Math.cos,
	exp:Math.exp,
	floor:Math.floor,
	ln:Math.log,
	pow:Math.pow,
	random:Math.random,
	round:Math.round,
	sin:Math.sin,
	sqrt:Math.sqrt,
	tan:Math.tan,
};

module.exports = M;